export const selectCatalogList = (state) => state.catalog.catalogList;
export const selectFilteredList = (state) => state.catalog.filteredCatalog;
export const selectFavoritesList = (state) => state.favorites.favoritesList;
export const selectIsLoading = (state) => state.catalog.isLoading;
export const selectShowModal = (state) => state.catalog.showModal;
export const selectModalItem = (state) => state.catalog.selectedItem;
